{
  "name": "flower-studio-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "yarn workspace @flower/server dev",
    "dev:admin": "yarn workspace @flower/admin dev",
    "dev:customer": "yarn workspace @flower/customer-h5 dev",
    "dev:florist": "yarn workspace @flower/florist-h5 dev",
    "build:server": "yarn workspace @flower/server build",
    "build:admin": "yarn workspace @flower/admin build",
    "build:customer": "yarn workspace @flower/customer-h5 build",
    "build:florist": "yarn workspace @flower/florist-h5 build"
  }
}
